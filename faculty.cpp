/*#include "faculty.h"

bool Faculty::operator== (const Faculty& other) const{
    bool match = (firstName == other.getFirstName()) && (lastName == other.getLastName()) && (address == other.getAddress());
    match = match && (city == other.getCity()) && (state == other.getState()) && (zip == other.getZip());
    match = match && (phone == other.getNumber()) && (phone == other.getEmail()) && (id == other.getID()) && (department == other.getDepartment());
}*/